import network from '../modules/network.js';

// Lists
const animeNames = document.getElementById('animeNames');
/**
 * @TODO make add <option> for search anime
 */
if (animeNames != 'undefined') {
    
};